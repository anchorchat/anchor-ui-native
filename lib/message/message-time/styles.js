import { StyleSheet } from 'react-native';

export default () => (
  StyleSheet.create({
    timeContainer: {
      paddingLeft: 4,
      flexDirection: 'row',
      alignSelf: 'flex-end'
    }
  })
);

import { StyleSheet } from 'react-native';

export default () => (
  StyleSheet.create({
    timeContainer: {
      paddingLeft: 4,
      marginTop: 2,
      flexDirection: 'row',
      alignSelf: 'flex-end'
    }
  })
);

import React from 'react';
import Menu from 'anchor-ui/menu';
import MenuItem from 'anchor-ui/menu-item';

const Nav = () => (
  <Menu>
    <MenuItem text="Getting started" onClick={() => {}} />
    <MenuItem text="Styleguide" onClick={() => {}} />
  </Menu>
);

export default Nav;
